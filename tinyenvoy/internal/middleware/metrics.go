package middleware

// metrics.go — Stats filter (Prometheus per-cluster counters).
// Implementation is in stats.go; this file exists as the canonical SPEC entry point.
// The NewStats function is declared in stats.go.
